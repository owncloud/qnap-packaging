---
version: "3"

services:
  owncloud:
    image: owncloud/qnap:10.7.0-amd64@sha256:2461652a2ee671802d78c5e4f45ba229b2e19374b2cd00e3548b910f42968dc3
    restart: always
    ports:
      - "127.0.0.1:11409:8080"
    depends_on:
      - db
      - redis
    environment:
      - OWNCLOUD_ADMIN_PASSWORD=admin
      - OWNCLOUD_ADMIN_USERNAME=admin
      - OWNCLOUD_DB_HOST=db
      - OWNCLOUD_DB_NAME=owncloud
      - OWNCLOUD_DB_PASSWORD=owncloud
      - OWNCLOUD_DB_TYPE=pgsql
      - OWNCLOUD_DB_USERNAME=owncloud
      - OWNCLOUD_MYSQL_UTF8MB4=true
      - OWNCLOUD_OVERWRITE_WEBROOT=/owncloud
      - OWNCLOUD_REDIS_ENABLED=true
      - OWNCLOUD_REDIS_HOST=redis
      - OWNCLOUD_UPDATE_CHECKER=false
      - OWNCLOUD_LICENSE_KEY=none
      - OWNCLOUD_LICENSE_CLASS=OCA\QNAP\QnapLicense
    volumes:
      - ${PWD}/data/owncloud:/mnt/data
      - ${PWD}/licenses/:/mnt/licenses/
      - ${PWD}/data/owncloud_tmp:/tmp
    healthcheck:
      test: ["CMD", "/usr/bin/healthcheck"]
      interval: 60s
      timeout: 30s
      retries: 5
    logging:
      driver: "local"

  db:
    image: amd64/postgres:13.3-alpine@sha256:b2db3c347304ad724f7f37c033aadebea805d2a25d674551fa78d716db08b650
    environment:
      - POSTGRES_DB=owncloud
      - POSTGRES_PASSWORD=owncloud
      - POSTGRES_USER=owncloud
    volumes:
      - ${PWD}/data/db:/var/lib/postgresql/data
    logging:
      driver: "local"

  redis:
    image: amd64/redis:6.2.4-alpine@sha256:b7cb70118c9729f8dc019187a4411980418a87e6a837f4846e87130df379e2c8
    restart: always
    volumes:
      - ${PWD}/data/redis:/data
    logging:
      driver: "local"
